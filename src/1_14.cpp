#include <cassert>
#include <iostream>
#include <vector>

/*
To A and B two list, B is A shuffle obtained, find the mapping used shuffle,
To be able to handle duplicate elements. Follow-up: Requirements space O (1)
*/

using namespace std;

void findMapping(vector<unsigned int>& mapping, const vector<int>& a,
                 const vector<int>& b)
{
}

int main(int argc, char* argv[])
{
}

