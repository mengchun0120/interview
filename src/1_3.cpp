#include <cassert>
#include <iostream>
#include <vector>
#include <string>

using namespace std;

// You are given with a large paragraph and N words. You have to find a min
// length subparagraph of the paragraph which contain all those N words in any
// order. Here length of a paragraph is the count of words in the paragraph.
bool findMinSubparagraph(const string& str, const vector<string>& words,
                         int& minSubparagraphLen, int& subparagraphStart,
                         int& subparagraphEnd)
{
    return true;
}

int main(int argc, char* argv[])
{
}
